# TODO

- [ ] UI 端 CheckAgent 可视化：模型调 CheckAgent 时，UI 把队友状态（在哪个 run、最近工具活动）渲染成可读卡片，而不是只有模型自己看到工具输出。
- [ ] SendMessage 增加 secret-request 载荷：凭据走掩码安全输入卡，值直达连接器凭据文件，不进转录和模型上下文。
- [ ] widget 增加 dismissOnMoveOn：低风险问题在用户继续发消息时自动收起。
